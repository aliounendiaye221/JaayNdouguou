import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jaayndougou.app',
  appName: 'JaayNdougou',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
