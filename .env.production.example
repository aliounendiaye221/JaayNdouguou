# Production Environment Variables for Vercel
# ===============================================
# CRITICAL: DO NOT commit this file with real values to git!
# These should be configured in Vercel Environment Variables
# https://vercel.com/docs/projects/environment-variables

# Database Configuration (PostgreSQL from Neon or Vercel)
# ========================================================
# Get these from your Neon database or Vercel Postgres
# Connection pooling URL (for serverless functions)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require&pgbouncer=true&connect_timeout=10"
# Direct connection URL (for migrations)
DIRECT_URL="postgresql://user:password@host:5432/database?sslmode=require&connect_timeout=10"

# NextAuth Configuration (REQUIRED)
# ==================================
# Your production domain (e.g., https://jaayndougou.app)
NEXTAUTH_URL="https://your-domain.vercel.app"
# CRITICAL: Generate a unique secret for production using:
# openssl rand -base64 32
# NEVER use the same secret in development and production!
NEXTAUTH_SECRET="GENERATE_WITH_openssl_rand_base64_32"

# WhatsApp Business Configuration (Public)
# =========================================
NEXT_PUBLIC_WHATSAPP_NUMBER="+221786037913"

# Site Configuration (Public)
# ============================
NEXT_PUBLIC_SITE_URL="https://your-domain.vercel.app"
NEXT_PUBLIC_SITE_NAME="JaayNdougou"

# Admin Configuration (First-time setup only)
# ============================================
# These are used to create the initial admin account on first deployment
# CRITICAL: Use a strong password (min 16 chars, mixed case, numbers, symbols)
# Change this immediately after first login!
ADMIN_EMAIL="admin@jaayndougou.sn"
INITIAL_ADMIN_PASSWORD="CHANGE_THIS_IMMEDIATELY_MIN_16_CHARS"

# Email Configuration (Optional - Required for sending emails)
# =============================================================
# Sign up at https://resend.com to get an API key
# RESEND_API_KEY="re_xxxxxxxxxxxxx"

# Payment Gateway Configuration (Optional)
# =========================================
# Configure these to enable online payment processing

# Wave Payment (Senegal)
# WAVE_API_KEY="your_wave_api_key_here"
# WAVE_MERCHANT_NUMBER="your_merchant_number_here"

# Orange Money (Senegal)
# ORANGE_MONEY_API_KEY="your_orange_money_api_key_here"
# ORANGE_MONEY_MERCHANT_NUMBER="your_merchant_number_here"
